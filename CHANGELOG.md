# Changelog

همهٔ تغییرات مهم این پروژه اینجا ثبت می‌شود. قالب بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/)
و نسخه‌گذاری بر اساس [Semantic Versioning](https://semver.org/).

## [1.20.0] - 2026-09-16

### Changed
- صفحهٔ اکانت‌ها یکپارچه + شروع جلسه و حذف اکانت

  حذف صفحهٔ تودرتوی جزئیات: با زدن روی هر اکانت همان صفحهٔ اکانت‌ها اطلاعات همان اکانت را نشان می‌دهد، دکمه‌های «▶️ شروع جلسه» و «🗑 حذف اکانت» (کنار افزودن) اضافه شد و بکاپ/ریستور هم در همان صفحه ماند.

## [1.19.0] - 2026-09-16

### Changed
- صفحهٔ جزئیات اکانت و سهمیهٔ دقیق، حذف پیش‌نیاز Freebuff

  با زدن روی هر اکانت فقط جزئیات همان اکانت در همان پیام نشان داده می‌شود (با دکمهٔ فعال‌سازی، بکاپ و ریستور)، سهمیهٔ اکانت‌های بدون جلسه دیگر عدد پیش‌فرض گمراه‌کننده نشان نمی‌دهد و فقط آخرین مقدار معتبر ثبت می‌شود، و بخش پیش‌نیاز نصب Freebuff از READMEها و install.sh حذف/اختیاری شد.

## [1.18.0] - 2026-09-16

### Changed
- بکاپ و ریستور اکانت‌ها، پرسش سوییچ با تایم‌اوت، پیام «اکانتی وصل نیست»

  افزودن چک نبود اکانت در /start و منوها، بکاپ/ریستور اکانت‌ها به‌صورت فایل JSON با دکمه و بکاپ روزانهٔ خودکار، و پیام سهمیهٔ تمام‌شده همراه دکمه‌های رفتن/انصراف و سوییچ خودکار پس از یک دقیقه بدون قطع‌شدن جلسه.

## [1.17.0] - 2026-09-16

### Changed
- تمدید خودکار جلسه و بهبود افزودن اکانت

  - افزودن اکانت: دکمهٔ «پیست credentials.json» حذف شد؛ فقط ورود با وب (+ نام دلخواه) می‌ماند.
  - صفحهٔ ورود وب حالا دکمهٔ «کپی آدرس لاگین» هم دارد.
  - دکمهٔ جلسه به‌صورت «۵۱ دقیقه مانده» نمایش داده می‌شود.
  - تنظیمات: دکمهٔ «تمدید خودکار جلسه» اضافه شد؛ ۵ دقیقه قبل هشدار می‌دهد و سر موعد جلسه را خودکار تازه می‌کند، با دکمهٔ لغو برای وقتی که کاربر نمی‌خواهد.

## [1.16.0] - 2026-09-16

### Changed
- پاک‌سازی خودکار منوهای قبلی

  با باز شدن هر منو، پیام و دکمه‌های منوی قبلی خودکار حذف می‌شوند تا چت شلوغ نشود. متن گفتگو (سؤال کاربر و پاسخ AI) دست‌نخورده می‌ماند. دکمهٔ تمدید اکنون در پیام جداگانه می‌آید تا پاسخ AI ویرایش/حذف نشود.

## [1.15.0] - 2026-09-16

### Changed
- README جدا برای هر زبان + معرفی فری باف

  حالا هر زبان یک فایل README جدا دارد: README.md (انگلیسی، پیش‌فرض)، README.fa.md (فارسی) و README.ru.md (روسی) با سوییچ زبان در ابتدای هر فایل. محتوای هر زبان تقریباً دو برابر و مفصل‌تر شد (قابلیت‌ها، نحوهٔ کار، سهمیه/باک، تایمر، ابزارهای سرور، اکانت‌ها و failover، امنیت، عیب‌یابی، معماری) و در ابتدای هر README بخش «دربارهٔ فری باف» با توضیح چندخطی و لینک https://freebuff.com اضافه شد.

## [1.14.1] - 2026-09-16

### Changed
- README سه‌زبانه و نصب‌کنندهٔ انگلیسی

  README به سه زبان بازنویسی شد: انگلیسی (پیش‌فرض/بالا)، فارسی (وسط) و روسی (پایین) با لینک تعویض زبان. متن install.sh هم کامل به انگلیسی ترجمه شد.

## [1.14.0] - 2026-09-16

### Changed
- سوییچ خودکار بین اکانت‌ها هنگام اتمام سهمیه

  وقتی سهمیهٔ اکانت فعال تمام شود، ربات خودکار به‌ترتیب روی اکانت بعدی سوییچ می‌کند، یک جلسهٔ تازه می‌سازد و پیام می‌دهد («سهمیهٔ اکانت X تمام شده؛ می‌رویم روی اکانت Y»). این failover در سه مسیر انجام می‌شود: خطای چت، ساخت جلسه، و دکمهٔ تمدید. اگر هیچ اکانتی سهمیه نداشت، پیام اتمام سهمیه با دکمه‌های افزودن/تغییر اکانت و خرید نمایش داده می‌شود.

## [1.13.1] - 2026-09-16

### Changed
- ترجمهٔ Freebucks به باک در ربات

  کلمهٔ Freebucks در همهٔ متن‌های ربات به «باک» تغییر کرد (در حالت انگلیسی: Bucks).

## [1.13.0] - 2026-09-16

### Changed
- رنگ و وضعیت مدل‌ها در منوی انتخاب

  منوی مدل‌ها حالا وضعیت هر مدل را نشان می‌دهد: مدل فعلی سبز، مدل‌های قابل‌انتخاب آبی، و مدل‌های در دسترس نبودن (خارج از لیست قیمت یا پر بودن سقف روزانه) با فیلد جدید disabled تلگرام خاکستری و غیرقابل‌کلیک. جلوی هر مدل هم مصرف (Freebucks/ساعت) و سهمیهٔ ساعتی امروز نوشته می‌شود.

## [1.12.1] - 2026-09-16

### Changed
- دکمهٔ تغییر اکانت و شفاف‌سازی سقف جلسه

  دکمهٔ «🔄 تغییر اکانت» کنار «➕ افزودن اکانت» زیر پیام اتمام سهمیه اضافه شد. خط «سقف تعداد جلسه» در وضعیت و اکانت‌ها با قالب استفاده‌شده/سقف (امروز/۷روزه/ماهانه) و توضیح «جدا از Freebucks» شفاف شد و در راهنما هم توضیح داده شد.

## [1.12.0] - 2026-09-16

### Changed
- هشدار سهمیه: افزودن اکانت و خرید اشتراک

  پیام اتمام سهمیه حالا هشدار ریسک/نقض قوانین افزودن اکانت را دارد به‌همراه قیمت پلن‌های پولی و تعداد جلسه، و دو دکمه زیر آن: «➕ افزودن اکانت» و «🛒 خرید اشتراک پولی». همین دکمه‌ها در مسیر تمدید و ساخت جلسه هم هنگام اتمام سهمیه نمایش داده می‌شوند.

## [1.11.3] - 2026-09-16

### Changed
- جایگزینی جلسه و پیام خطای تمدید

  کلمهٔ «جلسه» جای «سشن» در همهٔ دکمه‌ها و متن‌ها به‌کار رفت. دکمهٔ «تمدید جلسه» حالا اگر ناموفق باشد (مثلاً سهمیهٔ Freebucks تمام شده) دلیل واقعی را نشان می‌دهد: «سهمیهٔ Freebucks امروز تمام شده؛ ریست تا N دیگر» همراه لینک ارتقای پلن.

## [1.11.2] - 2026-09-16

### Changed
- دکمهٔ تمدید فقط نزدیک انقضا

  دکمهٔ «تمدید سشن» دیگر همیشه زیر پیام‌ها نیست؛ فقط وقتی کمتر از آستانهٔ هشدار (پیش‌فرض ۵ دقیقه، قابل تغییر از تنظیمات) تا انقضای سشن باقی مانده باشد نمایش داده می‌شود. دکمهٔ «منوی اصلی» هم از زیر پیام جواب حذف شد.

## [1.11.1] - 2026-09-16

### Changed
- وضعیت در پیام بالایی، جواب بدون سهمیه

  پیام بالایی حالا به‌جای تفکرات، وضعیت را نشان می‌دهد: نام مدل، زمان باقی‌ماندهٔ سشن و سهمیه (مانده/استفاده‌شده). متن تفکر نمایش داده نمی‌شود؛ فقط «در حال فکر کردن» و دستورهای در حال اجرا. جواب در پیام جداگانه و بدون سهمیه ارسال می‌شود.

## [1.11.0] - 2026-09-16

### Changed
- تفکرات زنده، ابزارهای بیشتر و جواب+سهمیه

  به‌جای «🤔 …» حالا پیام «💭 در حال فکر کردن…» نمایش داده می‌شود و با هر گام، تفکرات مدل و دستورهای اجراشده در همان پیام به‌روز می‌شوند؛ بعد جواب در پیام جداگانه و همراه وضعیت سهمیه (مانده/استفاده‌شده) ارسال می‌شود. ابزارهای مدل گسترش یافت: read_file، list_directory و write_file علاوه بر run_terminal_command. دستورهای خطرناک همچنان تأیید می‌گیرند.

## [1.10.0] - 2026-09-16

### Changed
- کنترل سرور توسط مدل (tool-calling)

  مدل حالا ابزار run_terminal_command دارد: میتواند روی همین سرور دستور اجرا کند و خروجی را ببیند (حلقهٔ ابزار تا ۸ گام). فقط دستورهای خطرناک (rm -rf، mkfs، dd، shutdown، curl|sh و…) قبل از اجرا با دکمهٔ اجرا/لغو تأیید میگیرند؛ بقیه خودکار اجرا میشوند. ربات root میماند. فعالسازی با ENABLE_SERVER_TOOLS (پیشفرض روشن) و پرسش در install.sh. دستور دستی /sh و دکمهٔ «اجرای دستور» هم هست.

## [1.9.0] - 2026-09-16

### Changed
- نمای شفاف سهمیه و اجرای دستور شل

  صفحهٔ اکانتها بازطراحی شد: برای هر اکانت «ماندهٔ Freebucks» و «استفادهشده» به‌صورت برجسته، تخمین ساعت با مدل فعلی، و سشن‌های باقی‌مانده (روز/هفته/ماه). دستور /sh <command> و دکمهٔ «⌨️ اجرای دستور» در منوی سرور اضافه شد تا کاربر بتواند مستقیم روی سرور دستور اجرا کند و خروجی را ببیند.

## [1.8.4] - 2026-09-16

### Changed
- ورود وب مقاوم‌تر: لینک جدید و بررسی تأیید

  به جریان افزودن اکانت با ورود وب دو دکمه اضافه شد: «🔄 بررسی تأیید» (پول فوری وضعیت) و «🔗 ساخت لینک جدید» (اگر سایت گفت لینک قبلاً استفاده شده). پیش‌نمایش لینک تلگرام غیرفعال شد تا کرالر تلگرام auth_code را مصرف نکند (علت پیام already used). مهلت انتظار به ۳۰ دقیقه افزایش یافت و وضعیت‌های غیرمنتظره لاگ می‌شوند.

## [1.8.3] - 2026-09-16

### Changed
- هشدار سهمیه در منوی مدل و ارسال خطا در پایین

  در صفحهٔ انتخاب مدل اگر Freebucks امروز تمام شده باشد، متن هشدار (با زمان ریست و لینک ارتقا) در پایین متن نمایش داده می‌شود. پیام خطا هم حالا به‌صورت پیام تازه در پایین چت ارسال می‌شود (نه ویرایش پیام بالایی) و ایموجی تکراری حذف شد.

## [1.8.2] - 2026-09-16

### Changed
- نمایش دقیق دلیل قطع سشن (سهمیه)

  وقتی سهمیهٔ Freebucks تمام شود، ربات دیگر با instance جعلی درخواست نمی‌فرستد و خطای ۴۲۸ نمی‌گیرد؛ به‌جایش دلیل واقعی را نشان می‌دهد: «سهمیهٔ امروز تمام شده؛ ریست تا N ساعت دیگر» + لینک ارتقای پلن. خطای rate_limited/spend_limited با زمان ریست از پاسخ سرور ترجمه می‌شود. زبان ربات هم به فارسی برگردانده شد.

## [1.8.1] - 2026-09-16

### Changed
- رفع خطای messages و تأیید قبل از ساخت سشن

  باگ Cannot read properties of null (reading messages) رفع شد (ensureSession شیء سشن برمی‌گرداند، نه نام). خطای 428 waiting_room_required حالا خودکار سشن را تمدید و یک‌بار دوباره تلاش می‌کند. برای جلوگیری از اسراف، اگر سشنی باز نباشد هنگام ارسال پیام، ابتدا پیام هشدار با دو دکمه «ایجاد سشن جدید و ارسال» و «انصراف» نشان داده می‌شود. خطاهای بک‌اند (428/409/403/429) به فارسی/انگلیسی ترجمه و نمایش داده می‌شوند.

## [1.8.0] - 2026-09-16

### Changed
- سوییچ زبان، سهمیهٔ ساعتی مدل‌ها و اصلاح دکمه‌ها

  دکمهٔ زبان (بین سرور و راهنما) برای سوییچ فارسی/English اضافه شد (رنگ پیش‌فرض/خاکستری). در صفحهٔ انتخاب مدل، کنار هر مدل قیمت (Freebucks/ساعت) و سهمیهٔ ساعتی امروز با آن مدل نمایش داده می‌شود. دکمهٔ پایین به‌طور کامل /start شد (هم نام هم اجرا). رنگ‌ها: راهنما آبی، دکمهٔ تایمر پایین سبز. بخش‌های اصلی رابط (وضعیت، مدل، تنظیمات، سشن‌ها، اکانت‌ها، راهنما) دوزبانه شدند.

## [1.7.2] - 2026-09-16

### Changed
- نوع پاسخ (mode) و دکمهٔ اکانت اصلاح شد

  حالت‌های پاسخ‌دهی به مقادیر واقعی فری‌باف تغییر کرد: 🧩 پیش‌فرض (DEFAULT)، ⚡ سریع (Lite)، 🛠 ساخت کامل (Build/MAX)، 🗺 برنامه‌ریزی (Plan) — جای AGENT/PRINT اشتباه. دکمهٔ منو از «👤 default» به «👤 اکانت‌ها» تغییر نام یافت تا با حالت پیش‌فرض اشتباه نشود، و سوییچ اکانت حالا در همان صفحهٔ اکانت‌ها می‌ماند و به صفحهٔ وضعیت نمی‌رود.

## [1.7.1] - 2026-09-16

### Changed
- شروع به‌جای استارت و حذف دکمه‌های تکراری

  دکمهٔ پایین «▶️ استارت» به «▶️ شروع» تغییر کرد و حالا دستور /start را اجرا می‌کند. سه دکمهٔ اینلاین تکراری (وضعیت/استارت/مدل) و دکمهٔ «⌨️ کیبورد» از منوی شیشه‌ای حذف شدند چون در کیبورد ثابت پایین وجود دارند. نصب به یک خط تبدیل شد و عنوان ریلیزها کوتاه و متن کامل در توضیحات قرار گرفت.

## [1.7.0] - 2026-09-15

### Changed
- راهنمای دکمه‌ای بخش‌به‌بخش (بدون لیست دستورات اسلش)، دکمه‌های رنگی بر اساس کارکرد (style)، توضیح کامل سهمیه/Freebucks و نمایش قیمت هر مدل

## [1.6.0] - 2026-09-15

### Changed
- کیبورد ثابت پایین تلگرام (Reply Keyboard) با سه دکمهٔ وضعیت/استارت/مدل + نمایش سهمیهٔ استفاده‌شده و مانده در وضعیت و بخش اکانت‌ها

## [1.5.1] - 2026-09-15

### Changed
- بازطراحی افزودن اکانت به‌صورت کاملاً دکمه‌ای: بدون نیاز به تایپ دستور، نام‌گذاری خودکار از ایمیل، دکمهٔ نام دلخواه

## [1.5.0] - 2026-09-15

### Changed
- افزودن اکانت با ورود وب: لینک لاگین رسمی فری باف از داخل ربات (/account add + دکمهٔ ورود با وب) و دریافت خودکار توکن، بدون نیاز به سرور یا نصب CLI

## [1.4.0] - 2026-09-15

### Changed
- قابلیت چند-اکانتی (استفادهٔ شریکی): افزودن/تعویض/حذف اکانت‌های فری‌باف با /account و منوی 👤، ذخیرهٔ امن در accounts/، سشن و سهمیهٔ مستقل هر اکانت

## [1.3.0] - 2026-09-15

### Changed
- بازطراحی منو: وضعیت/استارت/مدل در یک ردیف، دکمهٔ ثابت تایمر سشن در پایین، زیرمنوی واقعی تنظیمات (مود/تبلیغات/مدل/هشدار انقضا)، نمایش سهمیه حتی بعد از بسته‌شدن سشن

## [1.2.0] - 2026-09-15

### Changed
- تایمر سشن: نمایش زندهٔ زمان باقی‌مانده، هشدار خودکار ۵ دقیقه قبل، تمدید خودکار هنگام انقضا، دستور/دکمهٔ /renew و نمایش سهمیهٔ سشن و Freebucks

## [1.1.0] - 2026-09-15

### Changed
- نصب‌کنندهٔ تعاملی install.sh (دریافت توکن BotFather و آیدی عددی، چک پیش‌نیاز فری‌باف) + README با آدرس پروژهٔ اصلی https://freebuff.com

## [1.0.0] - 2026-09-15

### Added
- ربات تلگرامی نگهبان فری‌باف: چت، سشن‌ها، مدل، مود، تبلیغات، کنترل سرور و instance.
- رابط دکمه‌ای (inline) کامل با `/menu`.
- سوییچ واقعی مدل‌های رایگان (بستن سشن و admission مجدد).
- رفع خطاهای `free_mode_invalid_agent_model`، `free_mode_cli_required` و ۴۰۹ تداخل سشن.
- اسکریپت انتشار `release.sh` برای کامیت، تگ نسخه و GitHub Release.
